const config = {
      db: {
            host: 'localhost',
            user: 'admin',
            password: 'admin',
            database: 'gg_database',
            connectTimeout: 60000
      },
      listPerPage: 10,
};

module.exports = config;
